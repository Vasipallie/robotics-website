<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | GIIS Robotics Club</title>
    <style>
        @font-face {
            font-family: 'crest';
            src: url('/crst.otf') format('opentype');
        }
        @font-face {
            font-family: 'crestb';
            src: url('/crstb.otf') format('opentype');
        }
        html,body{
            width: 100%;
            height: 100%;
            margin: 0;
            background-color: #0a0a1d;
            color: #ffffff;
            font-family: 'crest';
            background-image: url('/logo/background.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }
        .id{
            font-size: medium;
            color: #ffffff;
            position: absolute;
            top: 12px;
            margin-left: 15px;
        }
        nav{
            padding-top: 5px;
            padding-inline: 10px;
            background-color: #0a0a1d;
            cursor: pointer;

        }
        img{
            margin: 0;
            padding: 0;

        }
        .main-content{
            backdrop-filter: blur(5px);
            width: 320px;
            height: 450px;
            margin: auto;
            margin-top: 100px;
            margin-bottom: 0;
            display: flex;
            flex-direction: column;
            border: 1px solid #a1a1a1;
            background-color: rgba(11, 0, 16, 0.746);
            border-radius: 10px;
            padding: 20px;
            gap: 0;         
        }
        .text{
            font-size: xx-large;
            font-family: 'crestb';
        }
        hr{
            border: 1px solid #d0d0d0;
            border-radius: 7px 7px 7px 7px;
            width: 100%;
        }
        
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 20px;
        }
        
        label {
            font-size: 16px;
            color: #ffffff;
            margin-bottom: 5px;
        }
        
        input[type="email"], input[type="password"] {
            padding: 12px;
            border: 2px solid #444;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.1);
            color: #ffffff;
            font-size: 16px;
            font-family: 'crest';
            transition: border-color 0.3s ease;
        }
        
        input[type="email"]:focus, input[type="password"]:focus {
            outline: none;
            border-color: #6c63ff;
            box-shadow: 0 0 8px rgba(108, 99, 255, 0.3);
        }
        
        input[type="email"]::placeholder, input[type="password"]::placeholder {
            color: #aaa;
        }
        
        input[type="submit"] {
            padding: 12px 20px;
            background: linear-gradient(135deg, #6c63ff, #4a47a3);
            color: #ffffff;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-family: 'crest';
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s ease;
            margin-top: 10px;
        }
        
        input[type="submit"]:hover {
            background: linear-gradient(135deg, #5a52d5, #3e3b8a);
            transform: translateY(-2px);
        }
        
        input[type="submit"]:active {
            transform: translateY(0);
        }
        .error {
            color: #ff4d4d;
            font-size: 14px;
            margin-top: 10px;
        }

        @media (max-width: 768px) {
            .main-content {
                width: 90%;
                max-width: 320px;
                margin-top: 80px;
                padding: 15px;
            }
            
            .text {
                font-size: x-large;
            }
            
            nav {
                padding: 10px;
            }
            
            .id {
                font-size: small;
                margin-left: 10px;
            }
        }

        @media (max-width: 480px) {
            .main-content {
                width: 95%;
                margin-top: 60px;
                padding: 10px;
            }
            
            .text {
                font-size: large;
            }
            
            input[type="email"], input[type="password"] {
                padding: 10px;
                font-size: 14px;
            }
            
            input[type="submit"] {
                padding: 10px 15px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body> 
    <nav onclick="window.location.href='/'"> <img src="/logo/logo-white.png" alt="GIIS Robotics Club Logo" width="30" height="30"> <span class="id">GIIS Robotics Club | <%= new Date().getFullYear() %> - <%= new Date().getFullYear() + 1 %></span></nav>
     
    <div class="main-content">
        <img src="logo/logo-white.png" alt="GIIS Robotics Club Logo" width="80" height="80">
        <span class="text">Admin Login</span>
        <% if (locals.error) { %>
            <span class="error"><%= locals.error %></span>
        <% } %>
        <hr>
        <form action="/login" method="POST">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" placeholder="Enter your registered email" required autocomplete="email">
            
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required autocomplete="current-password">
                
            <input type="submit" value="Login">
        </form>
    </div> 
        
</body>
</html>