<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GIIS Robotics Club</title>
    <style>
        @font-face {
            font-family: 'crest';
            src: url('/crst.otf') format('opentype');
        }
        html{
            width: 100%;
            height: 100%;
            margin: 0;
            background-color: #0a0a1d;
            color: #ffffff;
            font-family: 'crest';
        }
        /* anchor tags  --- logo ---- login url */
        ul{
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        li{
            float: left;
        }
        a{
            text-decoration: none;
            color: #ffffff;
            display: block;
            font-size: large;
        }
        nav{
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px clamp(20px, 4vw, 40px);
            position: relative;
            background: linear-gradient(to right, rgba(33,24,43,0.9), rgba(10,10,34,0.85));
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border-radius: 0 0 18px 18px;
            box-shadow: 0 8px 22px rgba(0,0,0,0.35);
            min-height: 68px;
        }
        
        nav ul {
            display: flex;
            flex: 1;
            gap: 6px;
        }
        
        nav logo {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
        }
        
        nav > a {
            flex: 1;
            text-align: right;
        }       
        .main-content {
            display: flex;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 80px); 
            flex-direction: column;
        }
        
        .center-logo {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .center-logo img {
            max-width: 200px;
            height: auto;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="about">About</a></li>
            <li><a href="links">Links</a></li>
            <li><a href="contact">Contact Us</a></li>
        </ul>
        <logo>
            <a href="/" ><img src="/logo/logo-white.png" alt="GIIS Robotics Club Logo" width="60" height="60"></a>
        </logo>
        <a href="/login">Login</a>
    </nav>
    
    <div class="main-content">
        <div class="header">
            
        </div>
        
    </div>
        
</body>
</html>